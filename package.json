{
  "name": "battle-royale-meme",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  },
  "dependencies": {
    "electron-is-dev": "^3.0.1"
  }
}
